<Window x:Class="Server.Service"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Server"
        mc:Ignorable="d"
        Title="Server" Height="400" Width="250">
    <Grid>
        <ListBox Margin="3" Name="lst"/>
        <!--ItemsSource="{Binding Path=cService.messHistory}">-->
        <!--<ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <TextBlock FontSize="16" Text="{Binding Path=User.UserName}" />
                        <TextBlock Text="{Binding Path=messageText}" />
                        <TextBlock Text="{Binding Path=Date, StringFormat=HH:mm}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>-->


    </Grid>
</Window>
